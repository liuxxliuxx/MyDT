服务器3：新版动力学训练（2026-09-09）

服务器3为 `lzh@172.27.246.188`，项目 `/data/lzh/MyDualTalk`，Python `/data/lzh/miniconda3/envs/lzh_DDG/bin/python`。使用唯一可用的 GPU0：`GPU-7dd2c83d-05f7-7260-31bb-1cb293f65f5c`。服务器1继续原生成流水线，本次只从它读取数据与已完成的上游导出。

运行目录为 `/data/lzh/MyDualTalk/runs/v3_2_adaptive_flow_20260909_gpu0`。

2026-09-09 16:30:22（北京时间），原始 EmotionTalk、上游权重、三来源 token 缓存与 DualTalk 合并 manifest 全部复制并通过逐项校验；token 文件总逻辑大小约 50.6 GB，与原始 EmotionTalk 的约 1.5 GB 分开计算。16:30:35 启动三数据集正式动力学训练，16:31:36 确认完成 10/10,000 步、累计 320 个新块，损失和梯度均有限。第 10 步跨维度作用范数为 `8.987e-5`，partner 反馈范数为 `0.03665`，说明新分支参与前向计算；尚不能据此判断长期预测效果。当前代码在服务器3再次通过 38 项测试，记录于 `final_regression.log`。

| 文件 | 内容 |
|---|---|
| `pipeline.log`、`status.json` | 数据依赖、检查与正式训练的阶段状态 |
| `transfer.log`、`transfer_status.json` | 从服务器1复制数据及逐项校验结果 |
| `environment.log`、`environment_status.json` | lzh_DDG 依赖安装及 CUDA 检查 |
| `early_regression.log` | 服务器3的 38 项兼容性测试 |
| `early_smoke.log`、`compiled_smoke.log` | 使用已校验 ET 缓存完成的两步 GPU 检查，不是三数据集正式实验 |
| `preflight_receipt.json` | 检查结果、代码哈希、上游哈希与实际训练预算 |
| `configs/dynamics.json`、`experiment_protocol.json` | 正式实验配置与范围，数据就绪后由启动器生成 |
| `dynamics.log` | 正式动力学训练输出 |
| `training/seed6666/dynamics/metrics.jsonl` | 每次日志输出及验证指标 |
| `training/seed6666/dynamics/best.pt`、`last.pt` | 验证选优与断点恢复权重 |

本轮隔离动力学改动：固定服务器1已经校准的共享情感表征，重新初始化状态核心和优化器，启用状态相关的跨维度作用及持续双人反馈。已有 event/action 头继续按动力学目标训练。完整条件输入与数据处理规则来自同一批 token 缓存。

上游为服务器1 `v3_1_endpoints_fix_20260909_gpu23/training/seed6666/calibration/best.pt`，文件 SHA256 为 `5e526af6a9418df31ebf1c591f2e4e05d98443702231cf1c2e221a05c815c8a2`。原 A0 gate 已通过、未绕过。校准 best 选在 2000 步，不代表校准只训练了 2000 步。

`train.learning_revision` 保留 `v3.1-clean-affect-causal-history`，`train.masking=null`，明确表示本轮没有训练新的 90/10 A0 遮挡策略。`train.dynamics_revision=v3.2-adaptive-dyadic-flow`、`state.flow_kind=adaptive_dyadic_v1`。新的遮挡策略仍是代码默认配置的一部分，需要另建 A0→校准→动力学实验评估。

正式预算为种子 6666、10,000 步、每步 32 个新块、单卡、TBPTT 最多 32 秒、未来查询 1/2/4/8/16/32 秒。单卡与服务器1原双卡运行的采样分配不同，不能宣称优化路径逐步相同。动力学 best 仍按原始 affect MSE 选择，独立 VAD、类别及强度指标需另外核对，不能根据 best 文件名推断情感语义最好。

为降低新版数值积分的计算开销，服务器3设置 `compile_adaptive_flow=true`，只编译系数计算函数，权重键名和模型构造保持一致。没有启用低精度 FP32 矩阵乘法选项。GPU 探针的最大预测差异为 `1.49e-8`，最大梯度差异为 `4.66e-10`；同样两步真实数据检查的验证 MSE，普通执行为 `0.0245809723`、编译执行为 `0.0245809772`。这些检查不能作为新模型情感效果的证据。

服务器3的空环境已安装 PyTorch `2.7.0+cu128`、Transformers `4.44.2`、NumPy `1.26.4`，与服务器1的主要模型框架版本对齐；`pip check` 与 CUDA 张量运算通过。EmotionTalk 原始处理数据 42,241 个文件已完整复制并校验。三来源 token 缓存约 49 GB，保留原始 manifest、train/val/test/OOD 划分和 DualTalk 的分片相对路径。

启动器 `scripts/server3_v32_dynamics.py` 等待环境和复制校验完成，核对上游特征及划分哈希、所有对话文件，再启动正式训练。已经通过且代码/预算未变化的检查会复用回执；依赖失败时明确停在失败状态。该脚本是本次训练的一次性流程，不是定时巡检任务。实时进度以服务器上的 `status.json`、`training_status.json` 和日志为准。
