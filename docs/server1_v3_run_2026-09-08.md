情感 Avatar v3 修改与服务器运行记录

记录日期：2026-09-08。服务器为 `s21_yhr@180.201.150.24`，工作目录为 `/home/s21_yhr/lzh/MyDualTalk`，解释器为 `/home/s21_yhr/miniconda3/envs/lzh_DDG/bin/python`。服务器 2 未修改。

**本次修改**

- 统一输入为最多一秒的双人 token 包，保留时间、角色、来源、有效长度与缺失信息。音频使用局部 token，文本在上下文编码前遮挡，训练遮挡重建；用户 FLAME 和 AU 使用独立适配器。Avatar 当前目标 FLAME 不进入观测。
- 校准前按特征来源、模型 revision 和 token 时钟选择已训练适配器。默认英文 DualTalk 使用 IEMOCAP domain 1 的 A/T 初始化 domain 2；不能只比较维度。视觉校准使用固定音频/文本教师。
- 长期状态统一到一个状态核心：双方各有相同 affect 坐标下的 fast、slow 和 baseline，关系保留两个方向。慢状态校正按实际秒数计算并限制速率，持久状态至少使用 FP32。事件去重、行为持续时间和观测缺失分别处理。
- A0、动力学和部署统一按真实秒数推进。未来预测只读取起点状态及查询时间，未来数据仅作为训练目标；条件预测需要声明未来输入。真实数据没有可靠跨文件关系时不拼接对话。
- 生成训练每次输出新增 25 帧，最多读取过去 75 帧。数值状态持续保留，计算图在 TBPTT 或优化器边界截断，最大 TBPTT 为 32 秒。生成阶段联合优化表达、表征保持和适用的未来预测目标。
- `none`、`affect`、`self`、`dyadic` 使用相同生成主干训练权限、原始输入窗口和每步 32 个有效新块。none 只训练生成器；affect 不更新长期状态；self/dyadic 才训练持久状态。生成器、观测器和状态分别裁剪梯度，避免辅助梯度改变生成主干的裁剪比例。
- checkpoint 保存完整构造、权重、来源、协议和恢复状态。评测使用 checkpoint 内配置，按误差和与有效元素数汇总，并保留跨块边界速度误差。

**代码与测试**

最终代码包为 `.transfer/v3_code_final_20260908.zip`，共 92 个文件，SHA256 为 `9838ab3d469b4971a271ae21340256dbc4b6bc4eaa5804f6327887a87e5fcf68`。

服务器代码覆盖前备份位于 `runs/v3_deploy_20260908_1910/deployment_backup`，部署清单位于 `runs/v3_deploy_20260908_1910/deployment.json`。原始数据和旧实验保留。

本地命令 `.venv/Scripts/python.exe -B -m pytest tests -q`：**161 passed，28.03 秒**。测试覆盖因果输入、目标隔离、时间组合、角色与会话、适配器来源、状态梯度、各对照优化权限、加权指标和 checkpoint 恢复。

服务器最终短跑为 `v3_smoke_final_20260908_gpu23`，复用已检查的新 token 缓存，重新执行 A0、FLAME 校准、动力学和四种生成对照，各训练两步。全部阶段于 2026-09-08 19:17:27 完成，`pilot_gate.json` 为 `passed: true`，无失败项。短跑使用 `allow_untrained: true`，跳过未训练表征的质量门槛，只检验代码链路及数值行为，不能证明情感或表情效果改善。

实际双卡日志确认：每步 32 个有效新块，第一步 800 有效帧、第二步因尾块为 790 帧；none 的辅助损失及 observer/state 梯度均为零，affect 的 state 梯度和 future_count 为零，self/dyadic 存在状态梯度；生成主干裁剪阈值统一为 5。短跑未出现混合精度溢出重试。

**正式运行协议**

物理 GPU 2：`GPU-5b497823-4a84-bde7-5670-2172ee96245d`。

物理 GPU 3：`GPU-9117039b-5194-5d46-d4a7-088ff06ce552`。

入口按这两个 UUID 限定设备。预处理并行使用两卡；训练和独立评测使用两个 DDP 进程。

正式运行会重新进行全量预处理，不复用短跑的数据子集。随后 seed 6666 执行 A0 10000 步、FLAME 校准 2500 步、动力学 10000 步及四对照各 1000 步诊断。数值和表征检查通过后，四组各独立训练 30000 步。seed 6667、6668 各自重训上游并执行四组 30000 步正式对照。每个正式对照结束后单独评测 test 和 OOD，均不用于选择 best。

**实际启动记录与日志位置**

正式流水线已于服务器时间 **2026-09-08 19:18:50** 启动，run ID 为 `v3_retrain_20260908_gpu23`，外层 PID 为 **1555027**。启动前确认最终短跑通过，物理 GPU2/3 无其他计算进程。

**2026-09-08 19:20:13 实际检查**：外层进程存活，当前阶段为 `preprocessing`；GPU2 上的预处理 PID 为 1555068，GPU3 上的 PID 为 1555069。EmotionTalk 已写出 37 个对话文件，IEMOCAP 已写出 23 个，日志正常推进。当前还未进入全量优化训练。此处为检查时的快照，后续进度以 `status.json` 和阶段日志为准。

命令为 `python -B scripts/server1_v3_pipeline.py --run-id v3_retrain_20260908_gpu23`，实际使用上述 conda 绝对解释器路径。没有传入 smoke、pilot-only 或复用短跑缓存参数。

以下为服务器绝对路径：

| 内容 | 路径 |
|---|---|
| 外层日志 | `/home/s21_yhr/lzh/MyDualTalk/runs/v3_retrain_20260908_gpu23/pipeline.nohup.log` |
| 当前阶段与失败原因 | `/home/s21_yhr/lzh/MyDualTalk/runs/v3_retrain_20260908_gpu23/status.json` |
| 启动参数及 PID | `/home/s21_yhr/lzh/MyDualTalk/runs/v3_retrain_20260908_gpu23/launch.json` |
| EmotionTalk 预处理 | `/home/s21_yhr/lzh/MyDualTalk/runs/v3_retrain_20260908_gpu23/preprocess_emotiontalk.log` |
| IEMOCAP 预处理 | `/home/s21_yhr/lzh/MyDualTalk/runs/v3_retrain_20260908_gpu23/preprocess_iemocap.log` |
| 后续阶段日志 | `/home/s21_yhr/lzh/MyDualTalk/runs/v3_retrain_20260908_gpu23/seed6666_*.log` |
| 权重及训练指标根目录 | `/home/s21_yhr/lzh/MyDualTalk/runs/v3_retrain_20260908_gpu23/training/` |
| 新 token 缓存 | `/home/s21_yhr/lzh/MyDualTalk/artifacts/v3_retrain_20260908_gpu23/` |

阶段权重生成后，路径格式为 `training/seed6666/{observation,calibration,dynamics}/best.pt` 和 `training/seed6666/{pilot,formal}/{none,affect,self,dyadic}/best.pt`。正式评测生成各 formal 目录的 `test.json`、`ood.json`。这些是后续产物位置，不代表当前已生成。

**仍需实验证明的内容**

防退化损失、方差监测和诊断门槛已经接入，但“编码器对真实情感有用”“历史和 partner 改善预测”“表情效果优于 none”仍需完整训练后的独立指标。诊断门槛只检查数值、梯度、预算及常量退化，不要求模型优于 baseline。

旧文本对齐报告有 6572 条文本流失败记录（5790 条低置信、782 条不支持符号，处理 5763 对）；这些文本继续标记缺失。v3 预处理会单独报告本次覆盖率。EmotionTalk 不伪造 arousal/dominance，身份未知的 IEMOCAP 视觉保持缺失。短对话不能证明分钟级记忆，句末情感标签也不复制成每秒真值。

实现及手动运行说明见 [v3_training.md](v3_training.md)。
