服务器3原 V3.2 动力学继续训练至20000步（2026-09-10）

已按用户要求从服务器3原实验的 `last.pt`（10000步）完整续训。9月10日18:41:02启动，18:44:04确认到10040步，无非有限损失或梯度，运行在GPU0，conda环境为 `lzh_DDG`。服务器1的 V3.2.1 分阶段训练未改动。本记录是启动验收，尚无续训后的验证结果。

本地记录位于 `runs/v3_2_adaptive_flow_extend20k_20260910_gpu0`，其中 `extension_protocol.json` 保存40次原始验证结果、数据规模和续训协议，`launch_health.json` 保存启动核验。

数据预算

| 数据源 | 训练记录数 | 一秒输入块数 |
| --- | ---: | ---: |
| EmotionTalk | 630 | 78228 |
| IEMOCAP | 90 | 24054 |
| DualTalk | 8766 | 217392 |
| 合计 | 9486 | 319674 |

10000步每步消耗32个新块，共320000块，相当于按现有采样器遍历约1.001轮。断点游标也确认完成一轮后进入第二轮第14个记录。20000步将消耗640000块，约2.002轮。DualTalk包含有向角色记录，不能将这些记录全部视为独立对话；同一目标还可能在不同起点的未来监督中重复出现。约一轮输入覆盖支持追加预算实验，不能单独证明此前欠拟合。

原实验的验证趋势

以下来自原16条验证记录上的 `learned_open_loop/affect_mse` 和 `last_state/affect_mse`，沿用原始有效元素加权方式，与服务器1新实验的宏平均指标不同。

| 步数 | 动力学MSE | 保持MSE | 动力学比保持高 |
| --- | ---: | ---: | ---: |
| 5000 | 0.002834487 | 0.002386619 | 18.77% |
| 6250 | 0.002088748 | 0.002133538 | -2.10% |
| 7750 | 0.002013469 | 0.002035335 | -1.07% |
| 9000 | 0.002529121 | 0.002147868 | 17.75% |
| 9500 | 0.003274036 | 0.002371840 | 38.04% |
| 10000 | 0.002145119 | 0.002004421 | 7.02% |

原 `best.pt` 为7750步。6250和7750步曾短暂超过保持，10000步未保持该优势。较早期整体差距缩小，但最后几千步波动明显。后续需要同时报告动力学绝对误差、保持绝对误差及配对差值，不把相对差距缩小直接解释为预测质量提高。

原实验在每次验证时用当前模型形成起点，因此保持对照的起点也会变化。完成续训后，在原验证协议外，应该将10000步与20000步权重放到相同冻结起点上比较，才能分离传播器改善与起点变化。追加预算由已查看的验证趋势决定，属于探索性续训；独立测试仍需预先固定权重选择规则。

续训保持的配置

- 原服务器3模型源码的独立副本，包括原自适应双人动力学及原损失。没有同步服务器1的新状态校准、固定起点训练或执行优化。
- 权重、AdamW状态（源优化器步数10000）、游标、持续状态、随机数和训练均值累计量均通过原完整恢复路径载入。
- 两组实际优化器学习率均为 `1e-4`，无新增学习率调度。每步32个新块，TBPTT为32秒，预测时距仍为1、2、4、8、16、32秒。
- 原模态缺失策略、BF16自动混合精度配置、编译系数执行方式和验证子集保持原样。每250步验证并保存；仍按原始情感向量MSE选best。
- 核验实际配置只改变 `train.max_steps`、`train.dynamics_steps` 和 `paths.output`。前两项均从10000改为20000。

完整恢复入口新增 `--resume-until-step` 和 `--resume-output`。前者必须是大于断点原停止上限及已完成步数的整数；只改外部配置中的预算仍不会覆盖断点配置。两个参数不能用于没有完整断点的新训练。

```bash
python -m emotion_ssm.train.dynamics_v3 \
  --config /path/to/original/config.json \
  --resume /path/to/last.pt \
  --resume-until-step 20000 \
  --resume-output /path/to/continuation
```

路径与恢复检查

```text
运行目录：/data/lzh/MyDualTalk/runs/v3_2_adaptive_flow_extend20k_20260910_gpu0
训练日志：运行目录/dynamics.log
运行状态：运行目录/status.json
训练步数：运行目录/training/seed6666/dynamics/training_status.json
最新断点：运行目录/training/seed6666/dynamics/last.pt
历史最佳：运行目录/training/seed6666/dynamics/best.pt
原一万步权重：运行目录/source/last.pt
原7750步最佳：运行目录/source/best.pt
代码副本：运行目录/code
```

原实验目录保留完成状态和全部文件，SHA256核验未被改写。续训目录携带原40次验证曲线与原best，后续日志从10001步继续追加；新best未出现时，best仍是原7750步权重。运行控制器只管理本次训练进程及完成/失败状态，没有创建定时巡检。

本地与服务器3各通过35项回归测试。新增恢复测试比较“一次训练4步”和“2步完成后延长到4步”，权重、优化器、持续状态、游标、随机数及指标逐项完全一致，且原断点文件未变。该数值一致性测试使用CPU；实际GPU启动已通过原恢复路径并推进到10040步，不能将CPU测试描述为GPU逐位一致性证明。
