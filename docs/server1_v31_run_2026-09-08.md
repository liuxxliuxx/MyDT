2026-09-08 服务器1 v3.1 修复与重训记录

后续监督修复：服务器时间2026-09-09 00:48停止本页原v3.1实验（step4060），修正IE arousal代理被当作独立intensity训练的问题。新实验 `v3_1_supervision_fix_20260909_gpu23` 于服务器时间01:08:05启动，总控PID1671795，仍仅GPU2/3、conda `lzh_DDG`。本地301项测试通过，真实双卡A0、校准、动力学与四种生成对照各两步链路测试通过。新实验重新初始化模型与优化器，ET/DT复用原完整特征根目录，IE使用经迁移及核验的新监督缓存；不会恢复旧optimizer。后续巡检以 `runs/v3_1_monitor/active_run.json` 为准，详细证据在 `docs/overnight_monitor_2026-09-08.md`。

新实验日志为 `/home/s21_yhr/lzh/MyDualTalk/runs/v3_1_supervision_fix_20260909_gpu23/pipeline.nohup.log` 和同目录 `seed6666_observation.log`。下文保留旧实验的启动与修复记录，其步数和指标不并入新实验。

旧实验 `v3_retrain_20260908_gpu23` 在 21:28:54 停止，保留第 4,250 步 best、第 5,000 步 last 和全部日志。新实验重新初始化、重新生成特征缓存，不接旧优化器。

本轮修复：

| 发现的问题 | 对应修改与验证 |
|---|---|
| Dropout 可以制造方差，raw-aff 范数可以掩盖归一化后的退化 | 在关闭 dropout、保留梯度的路径上约束最终单位 affect；相同输入不能降低方差惩罚；增加域内与全局统计 |
| token 重建绕过 affect head | 增加经过 affect 瓶颈的固定投影摘要重建；单独反传能测到 affect head 的非零梯度 |
| 句末标签只看到当前秒的末段声音 | 观测器保留最近 16 秒因果音频；生成主干仍只读取当前块和此前 3 秒原始上下文 |
| 原 40 ms 局部标准化删除能量，只保留卷积投影 | 改为独立 500 ms 完整冻结骨干，加入 8 维原始能量、幅度、过零率等统计；其中没有基频估计 |
| 文本同时间、同角色时没有语序 | 加入独立 token 位置编码；同一台词倒序会改变表征 |
| 批内对话单一、有效标签稀疏 | 混合多个对话和双方角色，优先抽取真实句末；按训练集频率设置类别权重，按 DDP 全局有效计数归一化 |
| 句内裁剪漏掉新增韵律 | Phase B 复用统一的端点裁剪；修改端点之后韵律不改变该端点监督 |
| 短音频的补零影响静音判定 | RMS 只计算有效采样，修改 padding 不改变观测和输出 |

A0 的学生和 EMA 分别使用真实验证标签评测。下游实际导出的权重与被选中模型逐张量核对。放行检查读取 `best.pt` 内指标，要求分类摆脱常量预测，并与只使用 train 计算的多数类和 VAD 均值对照比较。缺失模态单独记录。仅两步 smoke 显式允许跳过效果门槛。

服务器的预训练加载日志提示位置卷积参数未初始化，但实测旧 `weight_g/weight_v` 已通过 PyTorch hook 正确迁移，两因子与原文件差值均为 0。该警告不能作为权重丢失的证据；生成器和完整 checkpoint 的严格加载测试也已覆盖参数、有效卷积及前向输出一致性。

执行环境：`s21_yhr@180.201.150.24`，项目 `/home/s21_yhr/lzh/MyDualTalk`，conda `lzh_DDG`。训练仅允许物理 GPU2、GPU3，两卡 DDP。服务器2未修改。

正式流水线于 2026-09-08 22:18:13 启动，PID `1621805`，目录 `runs/v3_1_retrain_20260908_gpu23`。预处理进程为 `1621825`、`1621826`，物理 GPU2/3 已核对。启动时逐文件核验代码包 SHA-256 `44e7b0de0f2dc126980733c612aad2a1c951f7f3d5a3ee675122f8be291fb783`，清单共100个文件。后续索引启动修复通过独立部署审计记录。

22:30:03 补部署索引启动修复，代码包 SHA-256 `0eeb75b13e83e7f98745b103290f3f4e821ca97b34024fa6f22385ee6367e67e`，共101个文件。正式 train 共639,348个角色包，首次句末索引扫描较长；现在先经原子文件等待完成 CPU 索引，再初始化进程组。每个来源的缓存绑定 manifest 与训练文件签名，后续种子和校准直接复用。该修改未改变模型、监督或数据语义，部署时尚未进入优化器训练，也未停止已运行的预处理。服务器针对索引、A0、校准及恢复的43项回归通过。审计文件为 `runs/v3_1_retrain_20260908_gpu23/index_startup_fix.json`。

本地最终完整回归已通过222项测试（`.transfer/v31_pytest_final.log`）。服务器使用真实模型和两卡完成 A0、校准、动力学以及 none/affect/self/dyadic 各两步试运行，`pilot_gate.json` 为 passed。修正全局加权后，另行复跑两步校准也完成。旧式权重键检查只保留为测试工具，生产特征提取器继续使用经核查正确的标准预训练加载。

22:32 的正式预处理记录：EmotionTalk 已完成540个对话，IEMOCAP 已完成全部151个对话，日志无 traceback；流水线进程仍在运行，当前部署的101个文件哈希全部一致。此时GPU2继续处理EmotionTalk，GPU3已完成IEMOCAP，等待下一阶段的DualTalk双卡分片。全量数据准备与后续优化仍在运行；不能把预处理进度称为已经完成 A0 训练。服务器留存 `launch_verification.json`。

对当前生成的新缓存只读抽查 ET/IE 各3个对话，共925个一秒包、1850个角色包、210个句末监督点。ET 当前句子有效音频原子覆盖中位数4.244秒，IE为3.321秒；旧方案当前块可见时间上限中位数分别0.462秒、0.481秒。所有210个监督点都能读取前几个块，未发现 audio/prosody 时间或 mask 错位、包内未来 token 或句末裁剪后未来 token。该时长表示有效原子覆盖，不能解释为逐采样语音活动时长。此时尚未抽查正式 DualTalk 缓存，目标 FLAME 隔离已由单元测试及小规模流程覆盖。

服务器日志路径：

```text
/home/s21_yhr/lzh/MyDualTalk/runs/v3_1_retrain_20260908_gpu23/pipeline.nohup.log
/home/s21_yhr/lzh/MyDualTalk/runs/v3_1_retrain_20260908_gpu23/preprocess_emotiontalk.log
/home/s21_yhr/lzh/MyDualTalk/runs/v3_1_retrain_20260908_gpu23/preprocess_iemocap.log
/home/s21_yhr/lzh/MyDualTalk/runs/v3_1_retrain_20260908_gpu23/seed6666_observation.log
```

最后一项将在进入 A0 后生成。阶段状态位于同目录 `status.json`，阶段指标及 checkpoint 位于 `training/seed6666/`。两步 smoke、单元测试通过都不能证明表达指标优于 none；效果验收仍依赖完整重训与配对评测。
