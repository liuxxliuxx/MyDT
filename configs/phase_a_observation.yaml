SEED: 6666
DEVICE: cuda
DATA:
  ROOT: datasets
  EMOTIONTALK_ROOT: artifacts/features/emotiontalk_v2
  IEMOCAP_FEATURE_ROOT: artifacts/features/iemocap_v2
  IEMOCAP_FOLD: 5
  NUM_WORKERS: 8
TRAIN:
  OUTPUT_ROOT: runs
  EXPERIMENT_NAME: fold5
  BATCH_SIZE: 128
  EPOCHS: 50
  LR: 0.0003
  WEIGHT_DECAY: 0.0001
  AMP: true
  EMA_DECAY: 0.996
  SSL_ONLY_EPOCHS: 10
  AUGMENT:
    ENABLED: true
    AUDIO_NOISE_STD: 0.01
    AUDIO_MASK_RATIO: 0.10
    FACE_MASK_RATIO: 0.15
    TEXT_DROPOUT: 0.10
    MODALITY_DROPOUT: 0.15
    RELIABILITY_JITTER: 0.10
